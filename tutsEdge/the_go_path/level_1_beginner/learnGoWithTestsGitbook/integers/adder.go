package integers

// here we are writing just enough code to get our compiler to compile.
// we do this to make sure our tests are failing for the expected reason.
// to see that we need to get a compiled program first.

// Add takes two integers and returns the sum of them
func Add(x, y int) int {
	return x + y
}
